[project]
name = "frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mwaura-mwangi", email = "fmwauramwangi@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.62.0",
]

[project.scripts]
frontend = "frontend:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
